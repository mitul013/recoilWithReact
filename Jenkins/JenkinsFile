pipeline {
    agent any
    stages {
        stage("Build"){
            steps{
                dir('recoilWithReact'){
                    sh "npm install"
                }
            }
        }
    }
}
